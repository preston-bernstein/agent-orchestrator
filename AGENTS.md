# Agent instructions — agent-orchestrator

**Lane:** this repo only. No edits under `spring-api/` or `react-ui/` paths from here.

**Specs:** `docs/specs/<slug>/` — mirror vault `Orchestration PoC/Examples/docs/specs/` workflow. **NOT `fixtures/`** — that's runtime input for `--spec fixtures/<name>.md` (e.g. `fixtures/no-op.md` Phase 4 smoke).

**Quality:** **`pnpm run ci`** — same command **locally and in GitHub Actions** (parallel wave: typecheck, **`lint:base`** + **`lint:sonar`** (SonarJS), coverage, **`deps:cruise`**, **`debt:guard`** [lint/ts suppressions need ticket/`remove-by` nearby — see `scripts/debt-guard.mjs`], **`size:guard`**, **`structure:guard`**, **`architecture:guard`**; dependency-cruiser host Node outside DC range uses `npx node@22` then madge fallback; then Istanbul + fallow; then `mutation:t0` + `mutation:wide`). **Single strict profile:** Fallow **`health`** (**`maxCyclomatic` 10**, **`maxCognitive` 12**, Istanbul CRAP) on `src`/tests entries is canonical for structural risk; ESLint **`complexity` ≤ 10**, **`max-lines-per-function` ≤ 70**, and **`max-lines` ≤ 400** are hard errors for all `*.ts`/`*.tsx` (tests included) in `eslint/core.config.mjs`; **`size:guard`** enforces the maintainability target **`<=120` lines/file** across `src` + `tests` + `scripts` (allowlist-backed debt ledger). Sonar **`cognitive-complexity` ≤ 12** also applies to tests. See **`docs/specs/2026-05-05-clean-code-enforcement/strict-profile.md`**. Local one-shot ESLint (core + Sonar): **`pnpm run lint`**. Sequential ESLint like CI: **`pnpm run analyze:eslint`**. Package + graph gate: **`pnpm run analyze:deps`** (**`deps:cruise`**). Ordering / debugging: **`pnpm run ci:seq`**. Quick: **`pnpm run quality`** (no Stryker). Spot: `pnpm run test:run`. Coverage: `vitest.config.ts`. Stryker: `stryker.conf.json` (T0 slice) + `stryker.wide.conf.json` (**break ≥ 95**, **full `src/**`** minus T3 entrypoints; **equivalence mutators** listed in `mutator.excludedMutations` — see `_comment` there). Expand to full `check:fast` per vault Playbook Phase 1.

**Dependency graph:** `.dependency-cruiser.js` extends **`recommended-strict`** (severity **error**); **`not-to-unresolvable`** omitted (native addons e.g. better-sqlite3). **`layerForbidden`** adds curated **`src/`** boundaries (`runs`↛`workflows`, **`src/util`** leaf-only, `gates`↛`workflows`). Intent map: **`docs/specs/2026-05-05-clean-code-enforcement/layers.md`**. **`scripts/deps-validate.mjs`** runs **`deps:cruise`**.

**Duplication:** **Fallow** `duplicates.threshold` (**5**) = minimum **lines per duplicate block**. **`pnpm run fallow`** runs fallow with **`--production-dupes`** (duplicate scan skips test/fixture-heavy paths while retaining strict production clone detection).

**Vault:** `Development/Vibe Coding Hardening/Orchestration PoC/Build/` — prompts, patterns, Playbook.
