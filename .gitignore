node_modules/
dist/
coverage/
/runs/
.env
*.log
# Stryker work dirs (stryker.conf tempDirName + legacy)
.stryker-tmp*/
reports/mutation/

# Vault Build kit working copy (canon lives in Home Network Vault).
Orchestration PoC/
.fallow/
